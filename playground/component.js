/*
Higher Order Component(Function) 
 - is component(function) which takes another component(function) as argument and/or returns a component(function) as a value is called a higher order component(function) 

 Callback - WrappedComponent 
    - is a component being passed as a argument to another component is called as a wrapped component 

 EnhancedComponent - the returned component from an higher order component is called as enhanced component 

// to modifiy the props or add additional props(such as props.history or props.location or props.match) to a component 
*/